general:
  branches:
    ignore:
      - gh-pages

machine:
  timezone: Asia/Tokyo
  node:
    version: 5.1.0

test:
  override:
    - npm run compile

deployment:
  publish:
    branch: master
    commands:
      - git config --global user.name "CircleCI Deployment"
      - git config --global user.email "k.hamasaki+circleci@gmail.com"
      - npm run publish
